# PostgreSQL Database Password
# Used by both the postgres service and go-soc-consumer service
POSTGRES_PASSWORD=postgres

# Optional: Override default PostgreSQL configuration
# Uncomment and modify as needed for your environment
# POSTGRES_DB=smart-irrigation-system-db
# POSTGRES_USER=liwaisi-sis-admin

# Development/Production Environment
# Set to 'development' for local development or 'production' for deployment
# ENVIRONMENT=production

# Optional: Custom network configuration
# Uncomment if you need to modify the Docker network subnet
# DOCKER_SUBNET=172.20.0.0/16

# Optional: Custom port mappings
# Uncomment and modify if you need different port mappings
# POSTGRES_PORT=5432
# NATS_CLIENT_PORT=4222
# NATS_HTTP_MONITORING_PORT=8222
# NATS_MQTT_PORT=1883
# GO_SOC_CONSUMER_HTTP_PORT=8080