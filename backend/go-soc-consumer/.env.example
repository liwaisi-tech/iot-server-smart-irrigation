# Application uses in-memory storage by default - no database configuration needed

# MQTT Configuration (uses root NATS/MQTT service)
MQTT_BROKER_URL=tcp://localhost:1883
MQTT_CLIENT_ID=iot-go-soc-consumer
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_PORT=1883

# Application Configuration
HTTP_PORT=8080
HTTP_HOST=0.0.0.0
LOG_LEVEL=info
MIGRATIONS_PATH=./migrations

# Test Configuration
POSTGRES_TEST_DSN=postgres://liwaisi-sis-admin:${POSTGRES_PASSWORD:-postgres}@localhost:5432/smart-irrigation-system-db?sslmode=disable